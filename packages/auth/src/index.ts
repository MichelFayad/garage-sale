// @garage-sale/auth — shared auth primitives (roles, JWT) for web + mobile.
// Auth.js (NextAuth) provider config lives in apps/web (needs Next adapters).

export * from './roles.js';
export * from './jwt.js';
export * from './password.js';
